*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
.context/

# Generated by laravel/wayfinder from routes/*.php on every dev start and every build
# (see vite.config.ts). Committing them would mean a second source of truth for what the
# routes are, and a diff nobody reads.
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder

# A failing browser test writes the page it was looking at here, which is the first thing
# to open when one goes red. Removed again on a green run, so this only matters after a
# failure — and a screenshot of a broken console is not a thing to commit.
/tests/Browser/Screenshots
